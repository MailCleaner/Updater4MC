#!/bin/bash

export PATH="/usr/mailcleaner/bin:/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin"

apt-get clean
cd /var/lib/apt
rm -rf lists/*
mkdir lists/partial
apt-key update
apt-get clean
apt-get update
